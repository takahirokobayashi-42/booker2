<div class="post-body">
  <p>Title：<%= @post_book.title %></p>
  <p>Opinion：<%= @post_book.opnion %></p>
  <p>Username：<%= @post_book.user.name %></p>
  <p>Vote：<%= @post_book.created_at.strftime('%Y/%m/%d') %></p>
  <% if @post_book.user == current_user %>
    <%= link_to "deletea", @rocr, method: :delete %>
  <% end %>
</div>
